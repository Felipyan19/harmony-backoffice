# Neon Postgres pooled connection string. Never commit the real value.
DATABASE_URL=

# Neon Auth configuration.
NEON_AUTH_BASE_URL=
NEON_AUTH_COOKIE_SECRET=

# Temporary Auth.js credentials configuration.
# Remove these when the Neon Auth adapter replaces the provisional login.
AUTH_SECRET=
AUTH_ADMIN_EMAIL=
AUTH_ADMIN_PASSWORD_HASH=
