# PostgreSQL pooled connection string. Neon is the current provider, but the app targets standard PostgreSQL.
DATABASE_URL=

# Neon Auth configuration. These are the only auth-specific variables currently required.
NEON_AUTH_BASE_URL=
NEON_AUTH_COOKIE_SECRET=
